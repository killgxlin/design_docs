需求：
  desc
    set num
    set logon interval
    commit
  
  Session
    socket
    buffer
    Session
      this.prototype = new EventEmitter;
      socket = new Socket;
      socket.on('data', onData);
    onData(data){
      buffer += data;
      var msg = readMsg(buffer);
      if(msg == null) {
        buffer = "";
        return;
      }
      this.emit('msg', msg);
    }

  Robot 机器人
    session
    status

    logon 登录
      session.connect("localhost:9876", function(){
        CSMsg cslogon;
        session.sendMsg(sclogon);
      });
    createRole 创建角色
      CSMsg cscreaterole;
      session.send(cscreaterole);
    changeScene 切换地图
      CSMsg cschange;
      session.send(cschange);
    moveTo 随机移动
      CSMsg csmove;
      session.send(csmove);
    shout 喊话
      CSMsg cschat;
      session.send(cschat);
    logout 登出
      session.disconnect();
    Robot
      this.session = new Session
    handle_event
      sccreaterole -> createrole
      sclogon -> online
      chat -> online
      move -> online
      changescene -> online
      logoff -> offline
      
    do_state
      switch(state)
        case offline
          logon
        case createrole
          createRole
        case online
          switch random
            doShout
            doMove
            doChangeScene
            doLogoff

var robot = new Robot();
robot.start();
